# Evidence Files

`local-status.json` is a machine-readable snapshot from the local tuning
workspace. It records the selected candidate, bundle hash, promotion summary,
speed-gate summary, test counts, and live-deployment status at the time this
package was prepared.

The original large backtest archives are intentionally not included in this
public package.
